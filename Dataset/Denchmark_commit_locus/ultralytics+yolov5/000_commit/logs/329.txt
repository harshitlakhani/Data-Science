      Add file_size() function (#2911)          * Add file_size() function          * Update export.py 
         ts = torch.jit.trace(model, img, strict=False)          ts = optimize_for_mobile(ts)  # https://pytorch.org/tutorials/recipes/script_optimized.html          ts.save(f)         print(f'{prefix} export success, saved as {f}')         print(f'{prefix} export success, saved as {f} ({file_size(f):.1f} MB)')      except Exception as e:          print(f'{prefix} export failure: {e}')   
