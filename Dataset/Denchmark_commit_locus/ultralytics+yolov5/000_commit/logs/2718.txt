      Initial model ensemble capability #318 
                                   rect=rect,  # rectangular training                                    cache_images=cache,                                    single_cls=opt.single_cls,                                   stride=stride,                                   stride=int(stride),                                    pad=pad)        batch_size = min(batch_size, len(dataset)) 
