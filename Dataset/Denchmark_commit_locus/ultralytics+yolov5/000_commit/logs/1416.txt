      reverse plotting low to high confidence 
                     s += '%g %ss, ' % (n, names[int(c)])  # add to string                    # Write results                 for *xyxy, conf, cls in det:                 for *xyxy, conf, cls in reversed(det):                      if save_txt:  # Write to file                          xywh = (xyxy2xywh(torch.tensor(xyxy).view(1, 4)) / gn).view(-1).tolist()  # normalized xywh                          with open(txt_path + '.txt', 'a') as f: 
