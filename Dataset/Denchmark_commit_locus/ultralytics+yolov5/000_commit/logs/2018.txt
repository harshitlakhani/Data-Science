      Plot labels histogram colors (#3192) 
     # matplotlib labels      matplotlib.use('svg')  # faster      ax = plt.subplots(2, 2, figsize=(8, 8), tight_layout=True)[1].ravel()     ax[0].hist(c, bins=np.linspace(0, nc, nc + 1) - 0.5, rwidth=0.8)     y = ax[0].hist(c, bins=np.linspace(0, nc, nc + 1) - 0.5, rwidth=0.8)     [y[2].patches[i].set_color([x / 255 for x in colors(i)]) for i in range(nc)]  # update colors      ax[0].set_ylabel('instances')      if 0 < len(names) < 30:          ax[0].set_xticks(range(len(names))) 
