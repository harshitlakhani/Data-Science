      path fix for torch hub 
     Returns:          pytorch model      """     model = Model('models/%s.yaml' % name, channels, classes)     config = os.path.join(os.path.dirname(__file__), 'models', '%s.yaml' % name)     model = Model(config, channels, classes)      if pretrained:          ckpt = '%s.pt' % name  # checkpoint filename          google_utils.attempt_download(ckpt)  # download if not found locally 
