      Add Multi-Node support for DDP Training (#504)          * Add support for multi-node DDP          * Remove local_rank confusion          * Fix spacing 
     if last and not opt.weights:          print(f'Resuming training from {last}')      opt.weights = last if opt.resume and not opt.weights else opt.weights      if opt.local_rank in [-1, 0]:     if opt.local_rank == -1 or ("RANK" in os.environ and os.environ["RANK"] == "0"):          check_git_status()      opt.cfg = check_file(opt.cfg)  # check file      opt.data = check_file(opt.data)  # check file 
