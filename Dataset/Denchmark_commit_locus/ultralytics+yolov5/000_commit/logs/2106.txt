      pretrained model loading bug fix (#450)          Signed-off-by: Glenn Jocher <glenn.jocher@ultralytics.com> 
             plot_results()  # save as results.png          print('%g epochs completed in %.3f hours.\n' % (epoch - start_epoch + 1, (time.time() - t0) / 3600))       dist.destroy_process_group() if local_rank not in [-1,0] else None     dist.destroy_process_group() if local_rank not in [-1, 0] else None      torch.cuda.empty_cache()      return results   
