      Update inference multiple-counting (#2019)          * Update inference multiple-counting          * update github check 
                 # Print results                  for c in det[:, -1].unique():                      n = (det[:, -1] == c).sum()  # detections per class                     s += f'{n} {names[int(c)]}s, '  # add to string                     s += f"{n} {names[int(c)]}{'s' * (n > 1)}, "  # add to string                    # Write results                  for *xyxy, conf, cls in reversed(det): 
