      Update train.py (#2290)          * Update train.py          * Update train.py          * Update train.py          * Update train.py          * Create train.py 
     if rank in [-1, 0]:          # Strip optimizers          final = best if best.exists() else last  # final model         for f in [last, best]:         for f in last, best:              if f.exists():                 strip_optimizer(f)  # strip optimizers                 strip_optimizer(f)          if opt.bucket:              os.system(f'gsutil cp {final} gs://{opt.bucket}/weights')  # upload   
