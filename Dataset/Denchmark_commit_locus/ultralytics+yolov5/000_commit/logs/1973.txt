      Refactor test.py arguments (#3558)          * remove opt from test()          * pass kwargs          * update comments          * revert accidental default change          * multiple --img options          * add comments 
          plots=True,           wandb_logger=None,           compute_loss=None,          half=True,          opt=None):          half=True,  # FP16 half-precision inference          project='runs/test',          name='exp',          exist_ok=False,          task='val',          device=''):      # Initialize/load model and set device      training = model is not None      if training:  # called by train.py 
