      clean_str() function addition (#1674)          * clean_str() function addition          * cleanup          * add euro symbol �궗          * add closing exclamation (spanish)          * cleanup 
           n = len(sources)          self.imgs = [None] * n         self.sources = sources         self.sources = [clean_str(x) for x in sources]  # clean source names for later          for i, s in enumerate(sources):              # Start the thread to read frames from the video stream              print('%g/%g: %s... ' % (i + 1, n, s), end='') 
