      Multi-GPU disabled in test.py 
         if half:              model.half()  # to FP16           if device.type != 'cpu' and torch.cuda.device_count() > 1:             model = nn.DataParallel(model)         # Multi-GPU disabled, incompatible with .half()         # if device.type != 'cpu' and torch.cuda.device_count() > 1:         #     model = nn.DataParallel(model)        else:  # called by train.py          training = True 
