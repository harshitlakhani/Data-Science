      Refactor detect.py arguments (#3559)          * Refactor detect.py arguments          @SkalskiP @KalenMike          * unused ok          * comment arguments 
           # Inference          t1 = time_synchronized()         pred = model(img, augment=opt.augment)[0]         pred = model(img, augment=augment)[0]            # Apply NMS         pred = non_max_suppression(pred, opt.conf_thres, opt.iou_thres, opt.classes, opt.agnostic_nms,                                    max_det=opt.max_det)         pred = non_max_suppression(pred, conf_thres, iou_thres, classes, agnostic_nms, max_det=max_det)          t2 = time_synchronized()            # Apply Classifier 
