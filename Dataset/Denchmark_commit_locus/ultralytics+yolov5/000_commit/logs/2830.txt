      Precision-Recall Curve feature update (#1206)          * Precision-Recall Curve feature update          * sentinel value update 
     # Integrate area under curve      method = 'interp'  # methods: 'continuous', 'interp'      if method == 'interp':         x = np.linspace(0, 1, 101)  # 101-point interp (COCO)         x = np.linspace(0, 1, 1001)  # 101-point interp (COCO)          ap = np.trapz(np.interp(x, mrec, mpre), x)  # integrate      else:  # 'continuous'          i = np.where(mrec[1:] != mrec[:-1])[0]  # points where x axis (recall) changes          ap = np.sum((mrec[i + 1] - mrec[i]) * mpre[i + 1])  # area under curve       return ap     return ap, mpre, mrec      def bbox_iou(box1, box2, x1y1x2y2=True, GIoU=False, DIoU=False, CIoU=False, eps=1e-9): 
