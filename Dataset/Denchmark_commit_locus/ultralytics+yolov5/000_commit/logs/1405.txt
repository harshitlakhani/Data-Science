      update train.py gsutil bucket fix (#463) 
                     ema.update(model)                # Print             if local_rank in [-1, 0]:             if rank in [-1, 0]:                  mloss = (mloss * i + loss_items) / (i + 1)  # update mean losses                  mem = '%.3gG' % (torch.cuda.memory_cached() / 1E9 if torch.cuda.is_available() else 0)  # (GB)                  s = ('%10s' * 2 + '%10.4g' * 6) % ( 
