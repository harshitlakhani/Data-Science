      Remove redundant downloads mirror (#1461) 
                 y.append(r + t)  # results and times              np.savetxt(f, y, fmt='%10.4g')  # save          os.system('zip -r study.zip study_*.txt')         # utils.general.plot_study_txt(f, x)  # plot         # utils.plots.plot_study_txt(f, x)  # plot 
