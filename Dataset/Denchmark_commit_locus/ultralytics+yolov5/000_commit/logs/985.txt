      Update export.py with v3.0 Hardswish() support (#831) 
     # Update model      for k, m in model.named_modules():          m._non_persistent_buffers_set = set()  # pytorch 1.6.0 compatability         if isinstance(m, Conv):         if isinstance(m, Conv) and isinstance(m.act, nn.Hardswish):              m.act = Hardswish()  # assign activation          # if isinstance(m, Detect):          #    m.forward = m.forward_export  # assign forward (optional) 
