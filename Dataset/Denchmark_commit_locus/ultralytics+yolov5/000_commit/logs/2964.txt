      PyTorch Hub and autoShape update (#1415)          * PyTorch Hub and autoShape update          * comment x for imgs          * reduce comment 
   if __name__ == '__main__':      model = create(name='yolov5s', pretrained=True, channels=3, classes=80)  # example     model = model.fuse().eval().autoshape()  # for autoshaping of PIL/cv2/np inputs and NMS     model = model.fuse().autoshape()  # for PIL/cv2/np inputs and NMS        # Verify inference     from PIL import Image      img = Image.open('data/images/zidane.jpg')     y = model(img)     print(y[0].shape)     imgs = [Image.open(x) for x in Path('data/images').glob('*.jpg')]     results = model(imgs)     results.show()     results.print() 
