      update train.py gsutil bucket fix (#463) 
         # When in DDP mode, the generated indices will be broadcasted to synchronize dataset.          if dataset.image_weights:              # Generate indices.             if local_rank in [-1, 0]:             if rank in [-1, 0]:                  w = model.class_weights.cpu().numpy() * (1 - maps) ** 2  # class weights                  image_weights = labels_to_image_weights(dataset.labels, nc=nc, class_weights=w)                  dataset.indices = random.choices(range(dataset.n), weights=image_weights,                                                   k=dataset.n)  # rand weighted idx              # Broadcast.             if local_rank != -1:             if rank != -1:                  indices = torch.zeros([dataset.n], dtype=torch.int)                 if local_rank == 0:                 if rank == 0:                      indices[:] = torch.from_tensor(dataset.indices, dtype=torch.int)                  dist.broadcast(indices, 0)                 if local_rank != 0:                 if rank != 0:                      dataset.indices = indices.cpu().numpy()            # Update mosaic border 
