      Daemon thread plotting (#1561)          * Daemon thread plotting          * remove process_batch          * plot after print 
     plt.savefig('test_study.png', dpi=300)     def plot_labels(labels, save_dir=''): def plot_labels(labels, save_dir=Path(''), loggers=None):      # plot dataset labels      c, b = labels[:, 0], labels[:, 1:].transpose()  # classes, boxes      nc = int(c.max() + 1)  # number of classes 
