      PyTorch 1.7.0 Compatibility Updates (#1233)          * torch 1.7.0 compatibility updates          * add inference verification 
   if __name__ == '__main__':      model = create(name='yolov5s', pretrained=True, channels=3, classes=80)  # example     model = model.fuse().eval().autoshape()  # for autoshaping of PIL/cv2/np inputs and NMS      # Verify inference     from PIL import Image      img = Image.open('inference/images/zidane.jpg')     y = model(img)     print(y[0].shape) 
