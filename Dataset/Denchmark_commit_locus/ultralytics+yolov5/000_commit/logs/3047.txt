      Simplify autoshape() post-process (#1653)          * Simplify autoshape() post-process          * cleanup          * cleanup 
   if __name__ == '__main__':      model = create(name='yolov5s', pretrained=True, channels=3, classes=80)  # example     model = model.fuse().autoshape()  # for PIL/cv2/np inputs and NMS     model = model.autoshape()  # for PIL/cv2/np inputs and NMS        # Verify inference      from PIL import Image 
