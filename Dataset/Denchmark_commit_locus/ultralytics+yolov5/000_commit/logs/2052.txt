      Eliminate `total_batch_size` variable (#3697)          * Eliminate `total_batch_size` variable          * cleanup          * Update train.py 
             if not opt.notest or final_epoch:  # Calculate mAP                  wandb_logger.current_epoch = epoch + 1                  results, maps, _ = test.test(data_dict,                                              batch_size=batch_size * 2,                                              batch_size=batch_size // WORLD_SIZE * 2,                                               imgsz=imgsz_test,                                               model=ema.ema,                                               single_cls=single_cls, 
