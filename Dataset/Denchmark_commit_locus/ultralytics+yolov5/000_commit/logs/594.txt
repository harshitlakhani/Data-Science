      Add more logging for diagnostics 
             raise Exception('Error loading data from %s. See %s' % (path, help_url))            n = len(self.img_files)         print('Loaded %g images' % (n))          assert n > 0, 'No images found in %s. See %s' % (path, help_url)            bi = np.floor(np.arange(n) / batch_size).astype(np.int)  # batch index          nb = bi[-1] + 1  # number of batches   
