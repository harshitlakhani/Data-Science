      Add `train.run()` method (#3700)          * Update train.py explicit arguments          * Update train.py          * Add run method 
             # mAP              ema.update_attr(model, include=['yaml', 'nc', 'hyp', 'gr', 'names', 'stride', 'class_weights'])              final_epoch = epoch + 1 == epochs             if not opt.notest or final_epoch:  # Calculate mAP             if not notest or final_epoch:  # Calculate mAP                  wandb_logger.current_epoch = epoch + 1                  results, maps, _ = test.test(data_dict,                                               batch_size=batch_size // WORLD_SIZE * 2, 
