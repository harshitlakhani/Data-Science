      Cat apriori to autolabels (#1484) 
       # Return results      if not training:         print('Results saved to %s' % save_dir)         s = f"\n{len(list(save_dir.glob('labels/*.txt')))} labels saved to {save_dir / 'labels'}" if save_txt else ''         print(f"Results saved to {save_dir}{s}")      model.float()  # for training      maps = np.zeros(nc) + map      for i, c in enumerate(ap_class): 
