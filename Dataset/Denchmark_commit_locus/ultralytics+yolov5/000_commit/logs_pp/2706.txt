confusion matrix confusionmatrix normalize true fix
def plot self normalize true save dir savedir names try import seaborn normalize array self matrix self matrix sum reshape self normalize columns array self matrix self matrix sum reshape normalize else normalize columns arrayarray nan annotate would appear fig plt figure figsize tight layout tightlayout true
