minor import spelling updates
val len val val path abspath val isinstance val list else val val path path exists val print warning nwarning dataset found nonexistant paths val print warning nwarning dataset found nonexistent paths val len download script print downloading startswith http endswith zip url
