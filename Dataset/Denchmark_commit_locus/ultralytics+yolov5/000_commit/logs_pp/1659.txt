update mosaic border
labels self img size imgsize int random uniform range mosaic center border self mosaic border mosaicborder int random uniform border mosaic center indices index random randint len self labels range additional image indices index enumerate indices load image
