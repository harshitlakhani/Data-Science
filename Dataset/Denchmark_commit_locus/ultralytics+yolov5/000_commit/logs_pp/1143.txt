multi gpu multigpu test bug fix
print def forward self augment false profile false augment img size imgsize shape height width scales enumerate torch utils torchutils scale img scaleimg flip shape sameshape false flip fliplr scale torch utils torchutils scale img scaleimg shape sameshape false scale imwrite imgg jpg numpy transpose append self forward forwardonce scale img size imgsize flip scale return torch cat none augmented inference train else return self forward forwardonce profile single scale singlescale inference train def forward forwardonce self profile false outputs self model previous layer
