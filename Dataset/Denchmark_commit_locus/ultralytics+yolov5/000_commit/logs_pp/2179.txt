replace random affine randomaffine random perspective randomperspective signed signedoffby glenn jocher
mix mixup https arxiv org pdf pdf random random img labels load mosaic loadmosaic self random randint len self labels img labels load mosaic loadmosaic self random randint len self labels random beta mixup ratio alpha beta img img img astype uint img img img astype uint labels concatenate labels labels else
