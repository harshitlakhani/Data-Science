yol yolov aws inferentia inplace compatibility updates added flag enable disable inplace assignment operations removed shape print statements scope detect model import avoid circular dependency pep create descale pred descalepred replace lost space replace list tuple authored coauthoredby glenn jocher
def forward self augment false profile false augment img size imgsize shape height width scales none none flips outputs zip scale img scaleimg flip else int self stride max self forward forwardonce forward imwrite img jpg cpu numpy transpose save scale descale img size imgsize flip deflip elif img size imgsize flip deflip append return torch cat none augmented inference train return self forward augment forwardaugment augmented inference none else return self forward forwardonce profile single scale singlescale inference train def forward augment forwardaugment self img size imgsize shape height width scales none none flips outputs zip scale img scaleimg flip else int self stride max self forward forwardonce forward imwrite img jpg cpu numpy transpose save self descale pred descalepred img size imgsize append return torch cat none augmented inference train def forward forwardonce self profile false outputs self model
