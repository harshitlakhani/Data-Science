update datasets load images loadimages path improvements mixup
class load images loadimages inference def init self path img size imgsize path str path path agnostic osagnostic files path isdir path files sorted glob glob path join path elif path isfile path files path str path path agnostic osagnostic path abspath absolute path path isdir files sorted glob glob path join elif path isfile files else raise exception error exist images files path splitext lower img formats imgformats videos files path splitext lower vid formats vidformats len images len videos len images len videos self img size imgsize img size imgsize self files images videos self number files self video flag videoflag false true self number files self video flag videoflag false true self mode images videos self new video newvideo videos new video else self cap none assert self images videos found supported formats nimages snvideos path img formats imgformats vid formats vidformats assert self images videos found supported formats nimages snvideos img formats imgformats vid formats vidformats def iter self self count return self def next self self count self self count self raise stop iteration stopiteration path self filesself count
