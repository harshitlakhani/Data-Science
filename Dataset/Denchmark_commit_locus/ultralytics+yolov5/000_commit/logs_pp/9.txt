update expt name comment folder parsing training comment fix parsing fix evolve folder tqdm update train update train reinstate anchors meta dict anchor evolution working correctly reinstate logger prefer single line readout concise logging helps simplify notebook tutorials etc authored coauthoredby glenn jocher
increments directory runs exp runs exp comment expcomment number dir str path dir agnostic osagnostic sorted glob glob dir directories len max int xlen dir rfind path name else none increment dirs sorted glob glob dir directories dirs matches search exp dirs idxs int groups matches idxs max idxs increment return dir str comment comment else
