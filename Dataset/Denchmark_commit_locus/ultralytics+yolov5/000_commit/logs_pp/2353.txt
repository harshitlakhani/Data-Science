fix increment dir incrementdir use run xxx runxxx logdir fix increment dir incrementdir use run xxx runxxx logdir rerunning train logdir form run xxx runxxx causes index slicing max int xlen dir find path name else none increment underflow replace find rfind
dir str path dir agnostic osagnostic sorted glob glob dir directories len max int xlen dir find else none increment max int xlen dir rfind path name else none increment return dir str comment comment else
