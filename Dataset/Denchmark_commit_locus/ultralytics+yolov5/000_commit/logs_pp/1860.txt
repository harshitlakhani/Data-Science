utils general comment updates bug fixes
def coco single class labels cocosingleclasslabels path coco labels train label class labelclass makes single class singleclass coco datasets utils utils import coco single class labels cocosingleclasslabels makes single class singleclass coco datasets utils general import coco single class labels cocosingleclasslabels path exists new shutil rmtree new delete output folder makedirs new make new output folder
