cleanup
return self training else torch cat def forward self hasattr self inference output agi nyi nxi zip self grid self self stride self self self nxi nyi view self self nyi nxi permute contiguous view self sigmoid agi cls pcls self else self conf append cls pcls torch cat zip return torch cat scores boxes else dry run self self self self self self range self shape self self gridi self make grid makegrid repeat self view torch tensor self agi self anchor gridi anchorgridi repeat view self nxi self nyi return none staticmethod def make grid makegrid torch meshgrid torch arange torch arange
