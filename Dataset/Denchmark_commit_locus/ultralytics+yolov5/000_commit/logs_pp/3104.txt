check anchor order checkanchororder update
print analyzing nanalyzing anchors end model module model hasattr model module else model model detect shapes imgsz dataset shapes dataset shapes max keepdims true torch tensor concatenate zip shapes dataset labels float scale random uniform size shapes shape augment scale torch tensor concatenate zip shapes scale dataset labels float def metric compute metric none none knone
