new colors class
matplotlib use agg writing files def color list colorlist return first plt colors https stackoverflow com questions python color name rgb pythonfromcolornametorgb def hexrgb class colors ultralytics color palette https ultralytics com def init self self palette self hexrgb matplotlib colors tableaucolors values self len self palette def call self bgr false self paletteint self return bgr else staticmethod def hexrgb rgb order pil return tuple int return hexrgb matplotlib colors tableaucolors values base css xkcd colors colors create instance utils plots import colors def histd
