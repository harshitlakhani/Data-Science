improves docs handling entities resuming wandb logger wandblogger adds latest tag match wandb defaults adds entity handling last tag fixes bug causing finished runs resume removes redundant last tag wandb artifact
class wandb logger wandblogger log training runs datasets models predictions weights biases logger sends information wandb default information includes hyperparameters system configuration metrics model metrics basic data metrics analyses providing additional command line arguments train datasets models predictions also logged logger used see weights biases documentation https docs wandb com guides integrations yolov def init self opt name run runid data dict datadict job type jobtype training pre training pretraining routine self job type jobtype job type jobtype
