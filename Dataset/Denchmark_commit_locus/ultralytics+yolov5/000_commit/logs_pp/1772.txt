glob glob recursive true search
str path path agnostic osagnostic path abspath absolute path files sorted glob glob glob files sorted glob glob recursive true glob elif path isdir files sorted glob glob path join dir elif path isfile
