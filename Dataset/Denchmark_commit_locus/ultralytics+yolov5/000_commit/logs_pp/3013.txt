implement yaml safe load safeload implement yaml safe load safeload yaml safe dump safedump
assert len opt cfg len opt weights either cfg weights must specified opt img size imgsize extend opt img size imgsize len opt img size imgsize extend sizes train test opt name evolve opt evolve else opt name opt save dir savedir increment path incrementpath path opt project opt name exist existok opt exist existok opt evolve increment run opt save dir savedir str increment path incrementpath path opt project opt name exist existok opt exist existok opt evolve ddp mode opt total batch size totalbatchsize opt batch size batchsize
