colorstr updates import error importerror message fix colorstr updates colorstr updates colorstr default blue bold train magenta train blue
logger logging get logger getlogger name try import wandb except import error importerror wandb none logger info install weights biases experiment logging via pip install wandb recommended def train hyp opt device writer tbwriter none wandb none logger info colorstr blue bold hyperparameters join hyp items logger info colorstr hyperparameters join hyp items save dir savedir epochs batch size batchsize total batch size totalbatchsize weights rank path opt save dir savedir opt epochs opt batch size batchsize opt total batch size totalbatchsize opt weights opt global rank globalrank
