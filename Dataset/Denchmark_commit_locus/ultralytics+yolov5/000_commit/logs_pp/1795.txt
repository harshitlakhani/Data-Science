logging add hyperparameters logging add hyperparameters hyp bug fix image logging updates plots wandb cleanup wandb gitignore add cleanup cleanup move wandb import top file wandb evolve update import wandb run finish default anchors
cls int classesj color colorscls len colors cls namescls names else cls labels confj conf thresh labels confj conf thresh label cls labels else cls confj plot one box plotonebox box mosaic label label color color line thickness linethickness draw image filename labels paths none label path basename pathsi trim char paths label path pathsi name trim char size tsize get text size gettextsize label font scale fontscale thickness put text puttext mosaic label block blockx block blocky size tsize thickness line type linetype lineaa
