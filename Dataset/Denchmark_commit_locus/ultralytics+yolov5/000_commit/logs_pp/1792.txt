logging add hyperparameters logging add hyperparameters hyp bug fix image logging updates plots wandb cleanup wandb gitignore add cleanup cleanup move wandb import top file wandb evolve update import wandb run finish default anchors
assert len opt cfg len opt weights either cfg weights must specified opt img size imgsize extend opt img size imgsize len opt img size imgsize extend sizes train test opt name evolve opt evolve else opt name opt save dir savedir increment path incrementpath path opt project opt name exist existok opt exist existok increment run opt save dir savedir increment path incrementpath path opt project opt name exist existok opt exist existok opt evolve increment run ddp mode device select device selectdevice opt device batch size batchsize opt batch size batchsize
