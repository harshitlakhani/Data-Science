add variable stride variablestride inference support
print image self count self path end padded resize img letterbox img new shape newshape self img size imgsize img letterbox img self img size imgsize stride self stride convert img img transpose bgr rgb
