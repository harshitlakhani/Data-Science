unified project name results saving project name update update testing citesting yml address project path separator failure mode project name update address project path separator failure mode update testing citesting yml detect default name bug fix missing rstrip train exp train exp
return array targets def increment dir incrementdir dir comment increments directory runs exp runs exp comment expcomment number dir str path dir agnostic osagnostic path isdir dir stem dir sep removed path def increment path incrementpath path exist existok true sep increment path runs exp runs exp sep runs exp sep etc path path path agnostic osagnostic path exists exist existok path exists return str path else stem path dir stem dirs sorted glob glob dir directories dirs matches search stem dirs idxs int groups matches idxs max idxs increment return dir str comment comment else dirs glob glob path sep similar paths matches search sep path stem dirs int groups matches indices max else increment number return path sep update path plotting functions
