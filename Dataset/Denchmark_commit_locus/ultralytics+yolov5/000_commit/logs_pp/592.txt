update datasets
self label files labelfiles replace images labels replace path splitext txt self img files imgfiles read image shapes path replace txt shapes shapefile path try open read existing shapefile split read splitlines assert len shapefile sync except exif size exifsize image open tqdm self img files imgfiles desc reading image shapes savetxt fmt overwrites existing self shapes array dtype float rectangular training https github com ultralytics yolov issues self rect read image shapes path replace txt shapes shapefile path try open read existing shapefile split read splitlines assert len shapefile sync except exif size exifsize image open tqdm self img files imgfiles desc reading image shapes savetxt fmt overwrites existing sort aspect ratio array dtype float self shapes aspect ratio irect argsort self img files imgfiles self img filesi imgfilesi irect
