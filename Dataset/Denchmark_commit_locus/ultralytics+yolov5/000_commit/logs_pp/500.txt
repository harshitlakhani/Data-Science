inference update
run inference time time img torch zeros imgsz imgsz device device init img model img half half else img float device type cpu else none run model img half half else img device type cpu else none run path img ims vid cap vidcap dataset img torch numpy fromnumpy img device img img half half else img float uint
