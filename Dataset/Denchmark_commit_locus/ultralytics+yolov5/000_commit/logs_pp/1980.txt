updated coreml conversion pixel scaling
import coremltools print starting nstarting core coreml export coremltools version model convert inputs image type imagetype name images shape img shape convert convert model torchscript apply pixel scaling per detect model convert inputs image type imagetype name images shape img shape scale bias opt weights replace mlmodel filename model save print core coreml export success saved
